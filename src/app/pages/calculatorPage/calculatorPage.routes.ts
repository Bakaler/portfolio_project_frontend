import { Routes } from '@angular/router';
import { CalculatorPageComponent } from './calculatorPage.component';

export const calculatorPageRoutes: Routes = [
  { path: '', component: CalculatorPageComponent },
];